# SleepMapper Configuration

# Audio Parameters
sample_rate: 16000
n_mels: 128
hop_length: 512
n_fft: 2048
clip_duration: 30 # seconds

# Training Parameters
batch_size: 32
learning_rate: 1e-4
num_epochs: 50

# Inference Parameters
classification_threshold: 0.35